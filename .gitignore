# ===================== IDEA 专属文件 =====================
# IDEA 配置目录（包含项目设置、缓存、插件等）
.idea/
# 项目编译后的 class 文件（target 是 Maven/Gradle 编译目录）
target/
out/
# IDEA 生成的项目文件（非模块化项目）
*.iml
*.iws
*.ipr
# 本地配置文件（比如数据库密码、自定义配置）
*.properties.local
*.yml.local

# ===================== 通用忽略文件 =====================
# 编译生成的日志/临时文件
*.log
*.tmp
*.bak
# 操作系统临时文件
.DS_Store       # Mac
Thumbs.db       # Windows
# Maven/Gradle 本地缓存（无需提交）
.gradle/
build/          # Gradle 编译目录
# 本地测试/调试文件
*.jar
*.war
*.ear
# 敏感信息文件
*.key
*.pem
.env

# ===================== 前端（Vue）专属忽略 =====================
# Node.js 依赖目录（体积大，无需提交）
node_modules/
# 包管理锁文件（团队统一版本可酌情放开，默认忽略）
package-lock.json
yarn.lock
pnpm-lock.yaml
# Vue 构建输出目录
dist/
dist-ssr/
# 前端调试/日志文件
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
# 前端本地环境配置
.env.local
.env.development.local
.env.production.local
.env.test.local
# Vue 开发服务器临时文件
*.local

# ===================== 补充通用规则 =====================
# 临时文件/备份文件
*~
*.swp
*.swo
# 数据库本地文件（如 SQLite）
*.db
*.sqlite3
# 上传的图片/文件（本地测试用，生产需走存储服务）
src/main/resources/static/upload/
# 批处理/脚本执行日志
*.bat.log
*.sh.log